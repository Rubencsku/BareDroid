# Legacy Android-kernel template for wlan0.
# Mainline installations should start from setup_files/wpa_supplicant.conf.
ctrl_interface=DIR=/run/wpa_supplicant GROUP=netdev
update_config=1
# country=US  # Replace with the target's ISO 3166-1 alpha-2 country code.

# Copy this file to the target and add your own network block.
# Do not commit real credentials to the repository.
#
# network={
#     ssid="YOUR_WIFI_SSID"
#     psk="YOUR_WIFI_PASSWORD"
#     key_mgmt=WPA-PSK
# }
